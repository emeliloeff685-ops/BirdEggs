name: Build and Publish iOS App (Simple)

on:
  push:
    tags:
      - 'v*'  # Запускается при создании тега вида v1.0.0
  workflow_dispatch:  # Позволяет запускать вручную из GitHub UI

jobs:
  build-and-publish:
    runs-on: macos-14
    timeout-minutes: 60
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true
      
      - name: Install Fastlane
        run: gem install fastlane
        continue-on-error: true
      
      - name: Setup App Store Connect API Key
        env:
          APP_STORE_CONNECT_API_KEY_CONTENT: ${{ secrets.APP_STORE_CONNECT_API_KEY_CONTENT }}
        run: |
          mkdir -p ~/.appstoreconnect/private_keys
          echo "$APP_STORE_CONNECT_API_KEY_CONTENT" > ~/.appstoreconnect/private_keys/AuthKey.p8
          chmod 600 ~/.appstoreconnect/private_keys/AuthKey.p8
      
      - name: Setup Code Signing (if certificates provided)
        if: ${{ secrets.BUILD_CERTIFICATE_BASE64 != '' && secrets.BUILD_CERTIFICATE_BASE64 != null }}
        env:
          BUILD_CERTIFICATE_BASE64: ${{ secrets.BUILD_CERTIFICATE_BASE64 }}
          P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
          BUILD_PROVISION_PROFILE_BASE64: ${{ secrets.BUILD_PROVISION_PROFILE_BASE64 }}
        run: |
          # Создаём временный keychain
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
          CERTIFICATE_PATH=$RUNNER_TEMP/build_certificate.p12
          PP_PATH=$RUNNER_TEMP/build_pp.mobileprovision
          
          # Декодируем сертификат и provisioning profile
          echo "$BUILD_CERTIFICATE_BASE64" | base64 --decode > $CERTIFICATE_PATH
          echo "$BUILD_PROVISION_PROFILE_BASE64" | base64 --decode > $PP_PATH
          
          # Создаём keychain
          security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          
          # Импортируем сертификат
          security import $CERTIFICATE_PATH -P "$P12_PASSWORD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
          security list-keychain -d user -s $KEYCHAIN_PATH
          
          # Применяем provisioning profile
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp $PP_PATH ~/Library/MobileDevice/Provisioning\ Profiles
          
          # Получаем identity сертификата
          CERT_IDENTITY=$(security find-identity -v -p codesigning $KEYCHAIN_PATH | head -1 | grep -o '".*"' | sed 's/"//g')
          echo "CERT_IDENTITY=$CERT_IDENTITY" >> $GITHUB_ENV
          echo "KEYCHAIN_PATH=$KEYCHAIN_PATH" >> $GITHUB_ENV
          echo "✅ Code signing setup completed"
      
      - name: Build Archive
        env:
          DEVELOPMENT_TEAM: ${{ secrets.DEVELOPMENT_TEAM }}
        run: |
          echo "Building archive with Team ID: $DEVELOPMENT_TEAM"
          set -o pipefail
          
          # Определяем метод подписи
          if [ -n "$CERT_IDENTITY" ] && [ -n "$KEYCHAIN_PATH" ]; then
            echo "Using manual code signing with certificate: $CERT_IDENTITY"
            CODE_SIGN_STYLE="Manual"
            CODE_SIGN_IDENTITY="$CERT_IDENTITY"
            ADDITIONAL_FLAGS=""
          else
            echo "Using automatic code signing"
            CODE_SIGN_STYLE="Automatic"
            CODE_SIGN_IDENTITY=""
            ADDITIONAL_FLAGS="-allowProvisioningUpdates"
          fi
          
          # Собираем архив
          xcodebuild clean archive \
            -project BirdEggs.xcodeproj \
            -scheme BirdEggs \
            -archivePath $RUNNER_TEMP/BirdEggs.xcarchive \
            -configuration Release \
            CODE_SIGN_STYLE="$CODE_SIGN_STYLE" \
            ${CODE_SIGN_IDENTITY:+CODE_SIGN_IDENTITY="$CODE_SIGN_IDENTITY"} \
            DEVELOPMENT_TEAM="$DEVELOPMENT_TEAM" \
            $ADDITIONAL_FLAGS \
            2>&1 | tee build.log
          
          # Проверяем результат
          BUILD_EXIT_CODE=${PIPESTATUS[0]}
          if [ $BUILD_EXIT_CODE -ne 0 ]; then
            echo "❌ Build failed with exit code $BUILD_EXIT_CODE"
            echo ""
            echo "=== Build Log (last 100 lines) ==="
            tail -100 build.log
            echo ""
            echo "=== Checking for common issues ==="
            grep -i "error\|warning\|failed" build.log | tail -20 || echo "No specific errors found"
            echo ""
            echo "💡 Tip: If you see 'No Accounts' error, you need to:"
            echo "   1. Export your certificate (.p12) and provisioning profile"
            echo "   2. Add them to GitHub Secrets as BUILD_CERTIFICATE_BASE64 and BUILD_PROVISION_PROFILE_BASE64"
            exit 1
          fi
          
          echo "✅ Archive built successfully"
      
      - name: Export IPA
        env:
          DEVELOPMENT_TEAM: ${{ secrets.DEVELOPMENT_TEAM }}
        run: |
          # Проверяем наличие архива
          if [ ! -d "$RUNNER_TEMP/BirdEggs.xcarchive" ]; then
            echo "❌ Archive not found!"
            exit 1
          fi
          
          # Экспортируем IPA с автоматической подписью
          xcodebuild -exportArchive \
            -archivePath $RUNNER_TEMP/BirdEggs.xcarchive \
            -exportPath $RUNNER_TEMP/export \
            -exportOptionsPlist exportOptions.plist \
            -allowProvisioningUpdates \
            2>&1 | tee export.log || {
            echo "Export failed, checking logs..."
            cat export.log
            exit 1
          }
          
          # Проверяем наличие IPA
          if [ ! -f "$RUNNER_TEMP/export/BirdEggs.ipa" ]; then
            echo "❌ IPA file not found!"
            ls -la $RUNNER_TEMP/export/
            exit 1
          fi
          
          echo "✅ IPA exported successfully"
      
      - name: Upload to App Store Connect
        env:
          APP_STORE_CONNECT_API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_CONTENT: ${{ secrets.APP_STORE_CONNECT_API_KEY_CONTENT }}
        run: |
          echo "📤 Uploading IPA to App Store Connect..."
          
          # Проверяем наличие IPA
          if [ ! -f "$RUNNER_TEMP/export/BirdEggs.ipa" ]; then
            echo "❌ IPA file not found!"
            exit 1
          fi
          
          echo "IPA file size: $(du -h $RUNNER_TEMP/export/BirdEggs.ipa | cut -f1)"
          
          # Используем fastlane для загрузки через App Store Connect API
          fastlane deliver \
            --ipa "$RUNNER_TEMP/export/BirdEggs.ipa" \
            --api_key_path ~/.appstoreconnect/private_keys/AuthKey.p8 \
            --api_key_id "$APP_STORE_CONNECT_API_KEY_ID" \
            --api_issuer "$APP_STORE_CONNECT_ISSUER_ID" \
            --app_identifier "com.collector.eggscollector.BirdEggs" \
            --skip_screenshots \
            --skip_metadata \
            --skip_app_version_update \
            --force \
            --submit_for_review false \
            --verbose \
            2>&1 | tee upload.log || {
            echo "❌ Upload failed!"
            echo "Last 100 lines of upload log:"
            tail -100 upload.log
            exit 1
          }
          
          echo "✅ Upload completed successfully!"
      
      - name: Cleanup Keychain
        if: always() && env.KEYCHAIN_PATH != ''
        run: |
          security delete-keychain $KEYCHAIN_PATH || true
          rm -rf ~/Library/MobileDevice/Provisioning\ Profiles/* || true

